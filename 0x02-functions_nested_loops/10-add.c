#include "main.h"

/**
 * add - A function that adds two integers and returns the result.
 * @x: First parameter
 * @y: Second parameter
 *
 * Return: Result of the addition.
 */

int add(int x, int y)
{
	return (x + y);
}
